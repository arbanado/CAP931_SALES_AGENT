[project]
name = "cap931-sales-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "arbanado", email = "kacorriarbana@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "openai>=3.0.0",
    "pypdf>=6.16.1",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "streamlit>=1.61.1",
]

[project.scripts]
cap931-sales-agent = "cap931_sales_agent:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
